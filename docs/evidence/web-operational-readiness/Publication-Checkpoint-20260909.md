# Verified repair publication — 9 September 2026

The owner prioritised committing and pushing the verified current repairs to
`voltlead26-creator/PrompTED.AI`, branch `Thought-Enhanced-Document`, before
continuing the remaining generation and protected-editing improvements. This
checkpoint describes the candidate prepared from `e1d514ddda3b557a10ef1836a2fb7e1c6f327d1b`.
The commit containing this record is the publication identity; the local source
manifest records every selected path and byte digest separately.

## Implemented and verified locally

- Profile recovery, Supabase SDK dispatch, original document retention and
  upload/reopen paths have focused regressions and local browser evidence.
  The upload journey covered PDF, DOCX, RTF, Markdown, plain text, TextEdit
  UTF-16 variants and CSV, using real local Auth, RPCs and Storage with a
  controlled provider. Original hashes and independently read persistence
  were checked. This is not hosted account acceptance.
- tEdit review appears in the current viewport, with desktop/narrow-screen,
  keyboard and failed-apply recovery evidence. It remains an explicit review
  before a revision-bound edit is applied.
- Template policy, catalogue persistence, exact-wording audit identity and the
  private workspace save core have regression and historical upgrade evidence.
  The core is preparation for atomic generation finalisation; that finaliser
  is not activated by this publication.
- `db-20260908171929839-fa874a7a` passed all 80 migrations, 49 SQL files and
  2,573 assertions fresh; the 79-migration predecessor passed 2,511 assertions;
  the exact forward upgrade passed all 2,573 assertions again. Twenty-three
  real local Auth/PostgREST requests, independent receipt reads and preservation
  of historical rows/public function identity passed. The full web gate passed:
  165 deployment, 223 root, 382 shared and 1,035 web tests, lint, type checks,
  production build and progressive bundle checks. Cleanup and source stability
  checks passed.
- `upload-source-edge-gate-20260908162804040` passed 1,564 Edge tests with
  220 test steps, all 26 entry-point type checks and the focused source lint.
  Edge source and its runner still match that passing run.
- The publication-time production dependency audit passed with zero reported
  advisories across 384 dependencies.

The final runtime bytes match the latest database/web GREEN source snapshot.
The only subsequent executable configuration changes are the reviewed package
script and CI wiring for the exact 79-to-80 workspace-core acceptance mode.
The package command's no-service preflight passed. Independent review followed
86 local CI imports, fixture reads and helper dependencies without finding a
missing candidate dependency. Publication also requires exact index/worktree
digest equality, a credential scan and the final diff check. Reusing these
unchanged-source results does not claim that Linux CI has already passed.

The initial staged diff check reported a final blank line in two byte-pinned
historical inputs: the catalogue seed migration and catalogue upgrade baseline
JSON. Their verified bytes were preserved. Two literal `.gitattributes` entries
permit only `blank-at-eof` for those exact files; other whitespace checks remain
enabled. No SQL acceptance assertion, historical digest or runtime check changed.

## Deployment state and account testing

At `2026-09-08T17:51:44.531Z`, the existing Netlify production deployment
`6a9fc526610f2efb8c05454f` on `aiprompted`
(`f278cbcf-0161-43f7-a132-fd224aef2d9f`) was locked and read back as locked.
It still serves `e1d514d` at `https://ted.littlemissscarlett.co`.
Repository and branch linkage were revalidated before that reversible hold.
The hold prevents this Git push from automatically publishing a frontend
before its required backend changes are accepted. Site access controls and
the owner's existing Business allowance were not changed.

Fresh hosted inventory still shows 68 migrations and 25 functions. Required
`extract-upload`, `document-operation` and `brand-logo` are absent. The hosted
routing/capacity/evaluation attestations were freshly read at
`2026-09-08T17:57:47.788619Z`: all four routes remain unconfigured.
The current deployment contract also requires reconciliation of historical
undeclared/dormant endpoints. These are real release prerequisites; no values,
evaluation fingerprints or successful provider results have been invented.

**Verified in CI:** pending the exact pushed revision.
**Production exercised:** this candidate has not been released or exercised.
**Persistence proven:** the identified local synthetic workflows only.
**Export inspected:** earlier bounded DOCX compiler artifacts only; no general
format-preserving in-app PDF/Word editing or production export claim.
**Blocked/unverified:** full hosted generation/upload acceptance, complete
generation save/credit/reload finalisation, captured owner-edit eligibility,
catalogue-wide output assessment and general format-preserving editing.

Account usability must be checked on the actual accepted deployment. A Git push
or unchanged entitlement does not prove that the owner can complete the live
document journey. Backend release and the owner test flow remain the next work.
