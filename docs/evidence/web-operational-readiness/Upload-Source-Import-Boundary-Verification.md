# Source-aware upload import boundary

**Implemented; fresh database, exact historical upgrade and web gates verified locally. Browser integration remains pending.** The previous fallback settlement slice passed fresh/upgrade SQL, full web and1168 Edge tests. This slice remains on `/Users/kaichurchw/PrompTED.AI`, `Thought-Enhanced-Document`, HEAD `e1d514ddda3b557a10ef1836a2fb7e1c6f327d1b` plus the recorded overlay, under claim revision29. No hosted mutation or browser activation is authorised by these local tests.

## Existing authority and defect

Master Workspace consumes an extracted preview, heuristically rebuilds Section HTML, then submits it to the existing atomic `commit_document_import`. That command locks the owned upload and preserves committed replay, but otherwise accepts caller sections and can replace an existing same-owner document and delete/recreate its sections. It does not enforce the private source-only assessment or reject truncated previews. New v3 PDF/DOCX/RTF source output must not become an apparently formatting-preserved editable document through that path.

The existing `private.require_terminal_upload_before_import_commit` trigger already rejects unsettled ingest with `UPLOAD_IMPORT_REQUIRES_COMPLETED_INGEST`, rolling back attempted child writes. This is a preserved compatibility control, not a newly discovered incomplete-ingest bypass. The new regression retains the existing error rather than inventing a replacement. The smallest repair will extend this same trigger/function to relevant document-link changes and add the matching early v3 check in `commit_document_import` after committed replay and before child writes. Preserve existing v1/v2 behavior and unchanged historical links, and allow foreign-key cleanup to null.

No new source-only editable document is created in this milestone. Outcome-only attachments remain legitimate source context; Home/role commands must continue to work. Current browser/service upload DML is already revoked. Only the import command currently writes uploads.document_id/statuscommitted. Existing links are nullable and non-unique and will require further revision-bound work before they can authorise preserved editing.

## Regression and limits

`supabase/tests/upload_source_import_boundary.test.sql` positively seeds two owners and eight upload fixtures through real claim, retention-stage, checkpoint and settlement commands. DOCX/RTF use reviewed parser-manifest fixtures; the SQL suite does not run parsers or retain actual Storage bytes. The ready controls include complete v3 text and historical v2 DOCX; a separate v3 text fixture has genuinely unsettled classification. Five candidate imports test protected PDF/DOCX/RTF, truncated text and the existing readiness rejection. PDF attempts to replace an existing owned document, with full document/section/upload snapshots and counts checked afterward. Source-only RTF remains usable as outcome context.

Two privileged local UPDATE probes express the same document-link/status guard independently of the import RPC. Existing foreign-key deletion cleanup and another-owner rejection are also checked. These are disposable transaction fixtures, not hosted data operations. The expected unchanged-source result is71 assertions with11 failures and60 passes: four unsafe import acceptances, five dependent persistence/count failures and two missing table-boundary rejections. The unsettled classification test is expected to pass its existing guard. This prediction must be checked against actual output; it is not an observed result or11 independent product defects.

A read-only reviewer verified fixture UTF-16 character counts, manifests, RPC envelopes and role grants in the original67-assertion draft. The later four table/FK cases and preservation of the existing readiness error are being reviewed. No-service preflight `db-20260907084032263-10c5312d` passed before those final small assertion additions. `upload-source-import-red-inputs.json` records the final74-migration/44-test SQL inputs. No forward repair migration has been written.

## Required next product integration

Derive an owner-scoped retained-source list/detail projection from uploads and keep raw manifests private. Reopen originals on `/workspace?upload=<uuid>`, verify selected identity/owner before detail or download publication, and recover retention after classification/acknowledgement failure using the existing prepared upload identity. A UUID alone never proves retention. Protected sources must stay out of the generic HTML editor. RTF/v3 admission waits for these consumers. Exact raw TXT/MD representation and full DOCX/PDF editing/export remain required later slices; original retention is not their completion.


## Verified RED and forward repair — 19:03 Melbourne

`job-mtr0gb0p-0552c608`, evidence `db-20260907090045513-4d63c618`, exited1 in104 seconds. All2099 assertions in44 files ran. The new71-assertion file had exactly11 failures and60 passes:55–58 (protected sources/truncation),61–65 (dependent preservation/count consequences),67–68 (table document-link/status boundary). All2028 prior assertions passed. The existing unfinished-ingest rejection, positive import controls, committed replay, outcome-only attachment, FK cleanup and other-owner denial passed. This matches the prediction; these are not11 independent defects. Full `pnpm verify:web` passed. Cleanup/resources matched; completion confirmed HEAD and all816 source files unchanged under the DB runner exclusions.

Added `20260907112000_upload_source_import_boundary.sql`, SHA256 `16fe5041cf52351221a1318ab1bdbbc7006196683f72ed650fd253245569294a`. Its private assertion helper applies only to accepted v3 checkpoints, requires completed ingest using the existing error, rejects incomplete preview and permits only text in the current editable importer. Complete text is owner-reviewed wording, not an original-byte or Markdown round-trip attestation. Protected PDF/DOCX/RTF/XLSX source has no accepted preserving editor. The import command invokes that helper after immutable committed replay and before touching outcomes/documents/sections.

The same existing terminal-import trigger is extended to status and document_id updates, deriving eligibility from OLD accepted fields on new non-null assignment/reassignment or transition into committed. It retains the prior readiness branch unchanged. Unchanged history, null/v1/v2 behavior, outcome-only attachment and FK cleanup to null are preserved. There is no new persistence table, source binding column, public RPC, activation or data rewrite. Private helper execution is revoked from browser/service roles; the existing privileged commands invoke it. Web deployment requirements now include this migration. Migration validation passed75 files and deployment validation passed26 functions. Source hashes are recorded in `upload-source-import-forward-inputs.json`.

Fresh GREEN, historical upgrade (including already-committed protected v3 and null-version imports), browser source-list/detail/download integration and activation remain pending. Source-only browser imports still need a truthful original view before the new server acceptance version is enabled. No production or format-preserving editing/export completion is claimed.


No-service GREEN preflight `db-20260907090641795-da33eb3e` passed. A programmatic comparison confirms the import definition differs only by the early private helper call and its comment, and the existing late readiness branch is byte-identical. Independent source review found no blocker: nullable proof fails closed, OLD supplies the accepted checkpoint, replay/attachment/FK behavior remains ordered correctly, and existing ACLs are preserved. Fresh execution is next. Historical protected-v3 committed and null-version import compatibility remain required upgrade cases.


## Fresh GREEN verified and exact upgrade prepared — 19:10 Melbourne

`job-mtr0qly3-19d07a29`, evidence `db-20260907090846220-903545ee`, exited0 in104 seconds. All75 migrations and all2099 SQL assertions in44 files passed, including every71-assertion import-boundary case. Full `pnpm verify:web` passed. Cleanup/resources matched; independent completion inspection confirmed unchanged HEAD and all817 source files. There were no production JavaScript changes requiring another identical Edge rerun; its1168-test evidence remains scoped to the unchanged function source, while these new SQL behaviors have their separate database evidence.

The existing isolated runner/probe now has an exclusive `--upload-source-import-acceptance` mode pinned to102000→112000, including the reviewed predecessor/forward hashes and all current SQL. All66 plan/guard tests pass; prior v3, alias and fallback guards remain unchanged and reject this expanded migration set. Preflight `db-20260907091801137-78701650` passed before the final history-shape/assertion tightenings; final guard tests were rerun after them. The reviewed synthetic606-byte PDF is now included in the hashed/copied fixture inventory. Final input hashes are in `upload-source-import-upgrade-inputs.json`.

The prepared exercise uses16 real retained original objects, seven historical and nine current uploads. Before migration it genuinely commits a protected v3 RTF and a null-contract historical upload through authenticated PostgREST. Each carries distinct wording and the actual SectionVersion history shape. It saves a real pre-upgrade replay receipt, then captures complete uploads/outcomes/documents/sections. A separate ready RTF is retained for the new rejection path; it must not be confused with the immutable historical import.

After migration the exercise compares all four tables immediately, replays historical imports with changed proposed IDs/wording, rejects five new protected/truncated/unsettled imports without mutation, and positively imports complete v3 text/Markdown plus compatible v2 DOCX. It verifies five outcomes/documents/sections before context attachment, preserves source-only outcome use, rejects three privileged source-link/status/reassignment probes, and tests other-owner denial. Exact TextEdit document deletion must change only that document, its sections and its upload link; original bytes remain retained. These are controlled reviewed-wording and precomputed-extraction fixtures, not browser or preserving-editor proof. Final independent source review found no blocker to execution. The exact upgrade remains unexecuted at this entry.


## Exact upgrade verified — 19:29 Melbourne

`job-mtr1dk7g-175cddf4`, evidence `db-20260907092637052-f0f49175`, exited 0 in 145 seconds. All 75 current migrations and 2099 SQL assertions in 44 files passed on both the fresh database and the exact 102000 → 112000 upgrade. Full `pnpm verify:web` passed, including 297 shared and 896 web tests. The local Auth/PostgREST/Storage exercise recorded 251 HTTP checks, two authenticated owners, seven historical and nine current uploads, and 16 byte-checked original objects. Historical null-contract and protected v3 imports retained their complete table state and replay receipts. Five unsafe new imports were rejected, three supported imports persisted and replayed, source-only outcome attachment remained valid, and exact synthetic deletion preserved unrelated records and original bytes.

Completion inspection confirmed unchanged HEAD and all 817 source files under the runner exclusions, identical tracked before/after diff and resource inventories; `completion-comparison.json` records it. These are controlled extraction and reviewed-wording fixtures. Browser upload/reopen/download, source-preserving editing and export, CI and production remain unverified. The next slice exposes retained originals through owner-scoped reads in the existing Master Workspace, without promoting extraction previews into format-preserved editable documents.
