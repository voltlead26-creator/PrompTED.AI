# Web dependency patch verification — 9 September 2026

Target: `/Users/kaichurchw/PrompTED.AI`, `Thought-Enhanced-Document`.
Upload repair `4b0238e` was committed and pushed after its local database,
browser, web and Edge checks passed. This is the subsequent dependency repair;
it does not replace the upload result or claim production deployment.

The fresh production audit reports three advisories on the preceding lockfile:

| Package | Installed | Audit severity | Reviewed patch |
| --- | --- | --- | --- |
| Next.js | 15.5.22 | Critical, two advisories | 15.5.24 |
| Sharp | 0.35.0 | High, one advisory | 0.35.4 |

The upstream [Windows-hosted Next.js advisory](https://github.com/vercel/next.js/security/advisories/GHSA-p293-qw3h-jr36)
is specific to Windows filesystems. This is not evidence of that exploit on the
Mac/local or configured Netlify runtime. The [Next.js AVIF advisory](https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4)
and [Sharp libheif advisory](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c)
concern image processing. No exploit attempt or confirmed PrompTED compromise
was observed. The compatible supported patch versions above are available in
the npm registry and accept the locked Node 22.23.2 runtime.

Implementation pins Next.js and its ESLint configuration to 15.5.24 and updates
the existing Sharp override to 0.35.4. The lockfile must be regenerated by pnpm
10.33.0, without an arbitrary framework major upgrade or a new dependency path.

Initial verification ran in `job-mtu10xff-fc76d914` (11:40–11:42 UTC), against
`4b0238e` plus the package/lockfile overlay. Evidence is in
`db-20260909114012121-0cf5e0cd/`; the overall run exited 1.

- Installation passed with pnpm 10.33.0. Reviewed lockfile changes are the Next
  and Sharp families, their platform binaries, Sharp's optional Node types and
  WebAssembly runtime, and consolidation onto the existing caniuse-lite version.
- `pnpm audit --prod` passed: no known vulnerabilities found.
- Fresh database tests passed; source hashes before/after are retained.
- `pnpm verify:web` passed: lint, types, 423 shared tests, 1,035 web tests,
  Next.js 15.5.24 production build (29 pages), and progressive bundle checks.
- Browser discovery failed before running a journey. The local `.bin/playwright`
  launcher resolved to Deno's installation while `@playwright/test` imports
  resolved to pnpm's installation. Both were version 1.63.0, but separate runtime
  instances caused `Playwright Test did not expect test() to be called here`.

Both browser harnesses now resolve the exported Playwright CLI through the same
root dependency as their test imports and launch it with the verified Node
executable. A behavioral discovery regression with a replaced `.bin` launcher
passes; the focused runtime suite reports 18 passed, zero failed. This corrects
test execution without relaxing browser assertions or changing application code.

The repeat run `job-mtu1i53f-3bbead8e` completed successfully at 11:57 UTC.
Evidence is in `db-20260909115330354-4384f798/` and the concise
`Web-Dependency-Patches-Verification.json` receipt:

- Frozen install and production dependency audit passed.
- All 50 SQL files / 2,643 assertions passed on the disposable local database.
- Desktop and narrow Chromium journeys both passed without skips or retries:
  16 originals retained, independently downloaded and hash-checked; Markdown
  review/import/reload/replay and Profile save/reload/new-tab/recovery passed.
- The provider adapter recorded zero dispatches. Binary PDF/DOCX/RTF originals
  remain protected read-only previews; this does not prove preserving edits.
- Full web lint, types, shared/web tests and production build passed again.
- All recorded source hashes were unchanged during the run and matched the
  working copy when the result was inspected.

CI for upload commit `4b0238e` passed web, Edge and fresh database jobs but failed
the separate historical-upgrade preflight because its manifest still admitted
80 migrations and omitted the new upload migration/test. The separately reviewed
[79-to-81 upgrade repair](CI-Upgrade-Manifest-20260909.md) subsequently passed
local upgrade acceptance and another Chromium run. Exact final-commit CI and
hosted release prerequisites remain pending.

No credentials, hosted schema, function activation, Netlify publication or
account limit is changed by this dependency repair.
