# Production release prerequisites — 8 September 2026

**Publication update — 9 September 2026 AEST:** the owner explicitly prioritised
pushing the verified repairs before the remaining F2 improvements. The parent
refreshed the exact `aiprompted` site at `2026-09-08T17:51:44.531Z`: deployment
`6a9fc526610f2efb8c05454f` is now locked (`locked:true`), and the site, repository,
branch and existing `e1d514ddda3b557a10ef1836a2fb7e1c6f327d1b` production commit
were confirmed unchanged. No new web release is established by that lock or by
the instruction to push. The snapshot below is historical; its unlocked state
and earlier publication sequencing are superseded by this update. Its hosted
function/configuration observations retain their original timestamps and must
not be treated as a fresh hosted inventory. Remaining F2 implementation and
production release acceptance are separate from publishing the verified fixes.

This is a concrete preparation record, not approval to bypass a release gate. The owner authorised publishing reviewed updates to `voltlead26-creator/PrompTED.AI`, branch `Thought-Enhanced-Document`, and requested the application release. Local preparation continues in `/Users/kaichurchw/PrompTED.AI`. No new commit, push or web deployment has occurred. The existing Business allowance remains unchanged.

## Verified target and hosted state

- Supabase: `jjsykocqpjlekgsbylkd`, PrompTED, active/healthy, ap-southeast-2.
- Netlify: `aiprompted`, site `f278cbcf-0161-43f7-a132-fd224aef2d9f`, repository and canonical branch matched at `2026-09-08T12:44:15.591817+00:00`.
- Production URL: `https://ted.littlemissscarlett.co`.
- Published web deploy: `6a9fc526610f2efb8c05454f`, ready, published `2026-09-08T08:22:08.917Z`, commit `e1d514ddda3b557a10ef1836a2fb7e1c6f327d1b`. Build automation is active and the published deployment is not locked. A Git push can trigger publication, so the exact current deployment must be held before publishing an unreleased backend-dependent candidate. No such hold has been applied yet.
- Supabase still has 25 hosted functions. Active contract functions `extract-upload`, `document-operation` and `brand-logo` are missing. Current ingest remains version328. The current strict browser ingestion protocol requires the compatible new backend.
- At `2026-09-08 12:56:43.85467+00`, there were zero enabled captured activation pointers, zero enabled user assignments and zero captured operations. This is a current aggregate read, not permission to activate a cohort.

Read-only receipts: `hosted-release-refresh-20260908T1222.json`, `netlify-release-refresh-20260908T1242.json`, `captured-activation-refresh-20260908.json`.

## Inputs that cannot be invented

The current hosted attestation RPCs report all four OpenAI capacity and routing/evaluation routes unconfigured. The existing API key listed the requested Luna, Sol and Terra models at08:56UTC, but that read did not perform inference or prove funded availability, actual capacity, output quality, latency or cost.

The chosen source policy uses fast Luna-low, deep Sol-medium, research Terra-medium and review Sol-high. Before release, each needs actual project/model availability, approved model-sharing RPM/TPM allocation, a reviewed immutable route/suite snapshot, representative synthetic evaluation evidence, cost bounds and a real evaluation artifact fingerprint. Deep and review cannot independently claim the full shared Sol quota. The existing SQL configuration commands produce capacity attestations from real registered records. Evaluation fingerprints must come from actual reviewed results; random hashes or hashes of an unevaluated plan are invalid.

The exact fourteen OpenAI release-input names are in `.github/production/.env.example`: four model names, four capacity fingerprints, four evaluation fingerprints, routing version and evaluation suite version. Hosted runtime model/key/version configuration and GitHub deployment inputs are separate from the ignored local web dotenv. Remaining renderer, transport and webhook requirements retain their existing deployment-contract gates. No key, funding, budget, access, routing, capacity or evaluation record was changed in this preparation.

The owner has been asked where existing reviewed production capacity/evaluation configuration is stored. That information is pending. A paid synthetic evaluation, if required, still needs a concrete scoped evaluation plan and the applicable existing or new authority; it is not silently authorised by a successful model-list request.

## Existing endpoint reconciliation

The contract requires dormant functions to be absent and rejects undeclared hosted endpoints. Current hosted inventory includes:

| Endpoint | Current classification | Hosted version | JWT gateway verification |
| --- | --- | --- | --- |
| anthropic-messages | Undeclared | 109 | enabled |
| bcm-analyze-floor-plan | Undeclared | 10 | enabled |
| openai-chat | Dormant in current contract | 107 | enabled |
| openai-responses | Dormant in current contract | 108 | enabled |
| openai-stream | Dormant in current contract | 106 | enabled |

These functions were not deleted, relabelled or enabled through a new UI. Current source inventory alone cannot establish that no external or historical caller remains. Before retirement, retain their exact served source/JWT restoration material, review consumers and obtain any required exact endpoint-removal authority. Function-installation permission is not a reason to delete unrelated deployed functionality. The strict deployment check remains in place.

## Concrete release order

1. Finish the currently prepared legacy generation policy repair and its exact predecessor-to-current migration proof. Run focused regressions, all local web/Edge/database checks, actual upload/Profile/tEdit browser acceptance and the production dependency audit against one stable source manifest.
2. Review and explicitly stage the complete source set; exclude credentials, owner-access design, personal RTF files, generated bundles and raw private evidence. Freeze the exact manifest and verify all included dependencies.
3. Re-read the Netlify deployment immediately before holding it. Hold only the current production deploy, verify the hold, then commit and push the exact reviewed candidate using the authorised non-force canonical branch workflow. Keep the existing deployment held until backend, CI and release acceptance permit publication.
4. Obtain exact-commit CI, including the newly wired real browser and historical upgrade jobs. A workflow file alone is not CI proof.
5. Resolve the actual production inputs and endpoint reconciliation above. Revalidate hosted baseline, schema, functions, grants, Storage and existing operation versions immediately before mutation.
6. Apply only ordered compatible migrations, probe schema, deploy named contract functions in dependency order (`extract-upload` before `ingest-upload`), verify JWT/source identities, then deploy the approved exact web revision to the confirmed site. No destructive schema rollback.
7. Run authorised synthetic production acceptance with independent persistence reads, exact revision approval and actual artifact inspection where that capability is enabled. Record created versus downloaded versus inspected states. Clean only this acceptance run's exact synthetic resources.

## Product acceptance still separate

The upload work preserves and reopens original bytes. It does not yet establish general PDF/RTF format-preserving editing. The prepared DOCX compiler and source-unit mapping remain dormant pending in-app binding, persistence, approval and export eligibility. Catalogue/profile mismatches, captured v2 owner-entry eligibility and admission selection, reviewed output benchmarks, live-route evaluations, generated export inspection, utility endpoint boundary findings, and the broader outcome/action/security/performance matrix remain separate implementation or acceptance work.

A bounded upload/Profile/recovery milestone may be release-reviewed once its exact prerequisites pass. It must not be reported as the entire86-template application or flagship protected editing completed.
